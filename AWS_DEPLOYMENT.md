# 🚀 Развертывание EcoEats Bot на AWS

Это руководство поможет вам развернуть Telegram-бота на AWS для работы 24/7.

## 📋 Предварительные требования

1. Аккаунт AWS (зарегистрируйтесь на [aws.amazon.com](https://aws.amazon.com))
2. Токен Telegram бота от @BotFather
3. Git установлен на вашем компьютере
4. Базовые знания работы с терминалом

## 🎯 Варианты развертывания

### Вариант 1: AWS EC2 (Рекомендуется для начинающих)

EC2 - это виртуальный сервер в облаке AWS. Этот метод простой и понятный.

#### Шаг 1: Создание EC2 инстанса

1. **Войдите в AWS Console**
   - Перейдите на [console.aws.amazon.com](https://console.aws.amazon.com)
   - Войдите в свой аккаунт

2. **Запустите EC2 инстанс**
   - В поиске введите "EC2" и выберите сервис
   - Нажмите "Launch Instance" (Запустить инстанс)

3. **Настройте инстанс:**
   - **Name**: `ecoeats-bot`
   - **OS**: Ubuntu Server 22.04 LTS (Free tier eligible)
   - **Instance type**: t2.micro (Free tier eligible - бесплатный год)
   - **Key pair**: Создайте новую пару ключей:
     - Нажмите "Create new key pair"
     - Name: `ecoeats-bot-key`
     - Type: RSA
     - Format: .pem (для Linux/Mac) или .ppk (для Windows/PuTTY)
     - **ВАЖНО**: Сохраните файл ключа - он понадобится для подключения!

4. **Network settings:**
   - Поставьте галочку "Allow SSH traffic from" → выберите "My IP"
   - Остальное оставьте по умолчанию

5. **Storage**: 8 GB (по умолчанию, достаточно для бота)

6. Нажмите **"Launch Instance"**

#### Шаг 2: Подключение к серверу

**Для Windows:**
```powershell
# Если у вас Windows 10/11, используйте PowerShell или CMD
# Замените путь к ключу и IP-адрес вашего сервера
ssh -i "C:\path\to\ecoeats-bot-key.pem" ubuntu@YOUR_EC2_PUBLIC_IP
```

**Для Linux/Mac:**
```bash
# Сначала установите правильные права на ключ
chmod 400 ecoeats-bot-key.pem

# Подключитесь к серверу
ssh -i ecoeats-bot-key.pem ubuntu@YOUR_EC2_PUBLIC_IP
```

*Найти Public IP можно в AWS Console → EC2 → Instances → выберите ваш инстанс*

#### Шаг 3: Установка необходимого ПО на сервере

После подключения к серверу выполните следующие команды:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавление пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker ubuntu

# Установка Docker Compose
sudo apt install docker-compose -y

# Установка Git
sudo apt install git -y

# Перезапустите сессию для применения изменений
exit
```

Подключитесь снова:
```bash
ssh -i ecoeats-bot-key.pem ubuntu@YOUR_EC2_PUBLIC_IP
```

#### Шаг 4: Загрузка проекта

```bash
# Клонируйте ваш репозиторий (замените на ваш URL)
git clone https://github.com/YOUR_USERNAME/ecoeats_bot.git

# Перейдите в директорию проекта
cd ecoeats_bot
```

#### Шаг 5: Настройка переменных окружения

```bash
# Создайте файл .env на основе примера
cp .env.example .env

# Отредактируйте файл и добавьте ваш токен
nano .env
```

В редакторе nano:
1. Замените `your_bot_token_here` на ваш реальный токен от @BotFather
2. Нажмите `Ctrl + X`, затем `Y`, затем `Enter` для сохранения

#### Шаг 6: Запуск бота

```bash
# Сборка и запуск контейнера
docker-compose up -d

# Проверка логов
docker-compose logs -f

# Чтобы выйти из просмотра логов: Ctrl + C
```

#### Шаг 7: Настройка автозапуска

Бот уже настроен на автоматический перезапуск (параметр `restart: unless-stopped` в docker-compose.yml). 
Он будет автоматически запускаться после перезагрузки сервера.

#### Управление ботом

```bash
# Остановить бота
docker-compose down

# Запустить бота
docker-compose up -d

# Перезапустить бота
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Обновление бота (после git push)
git pull
docker-compose up -d --build
```

---

### Вариант 2: AWS Lightsail (Проще и дешевле)

AWS Lightsail - упрощенная версия EC2, идеальна для небольших проектов.

#### Шаг 1: Создание Lightsail инстанса

1. Перейдите на [lightsail.aws.amazon.com](https://lightsail.aws.amazon.com)
2. Нажмите "Create instance"
3. Выберите:
   - **Platform**: Linux/Unix
   - **Blueprint**: OS Only → Ubuntu 22.04 LTS
   - **Instance plan**: $3.50/месяц (512 MB RAM) или $5/месяц (1 GB RAM - рекомендуется)
   - **Instance name**: `ecoeats-bot`
4. Нажмите "Create instance"

#### Шаг 2: Подключение

1. В консоли Lightsail нажмите на имя инстанса
2. Перейдите на вкладку "Connect"
3. Нажмите "Connect using SSH" - откроется терминал в браузере

#### Шаг 3-6: Следуйте тем же шагам, что и для EC2

(Шаги по установке Docker, клонированию репозитория и запуску бота идентичны)

---

### Вариант 3: AWS ECS Fargate (Для продвинутых пользователей)

Полностью управляемый контейнерный сервис без необходимости управлять серверами.

#### Краткая инструкция:

1. Создайте ECR репозиторий для Docker образа
2. Загрузите образ в ECR
3. Создайте ECS Cluster (тип: Fargate)
4. Создайте Task Definition с вашим образом
5. Создайте ECS Service для постоянной работы
6. Настройте переменные окружения в Task Definition

Подробная инструкция требует продвинутых знаний AWS.

---

## 💰 Стоимость

### EC2 t2.micro
- **Free tier**: 750 часов/месяц бесплатно первый год
- **После free tier**: ~$8-10/месяц

### Lightsail
- **$3.50/месяц**: 512 MB RAM, 1 vCPU, 20 GB SSD
- **$5/месяц**: 1 GB RAM, 1 vCPU, 40 GB SSD (рекомендуется)

### ECS Fargate
- ~$15-20/месяц для минимальной конфигурации

---

## 🔒 Безопасность

1. **Никогда не коммитьте .env файл в Git!** (уже в .gitignore)
2. **Регулярно обновляйте сервер:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```
3. **Используйте AWS Systems Manager Session Manager** вместо SSH (более безопасно)
4. **Настройте CloudWatch** для мониторинга логов

---

## 📊 Мониторинг

### Просмотр логов бота

```bash
# Последние 100 строк
docker-compose logs --tail=100

# В реальном времени
docker-compose logs -f

# Только ошибки
docker-compose logs | grep ERROR
```

### Проверка работы бота

```bash
# Статус контейнера
docker ps

# Использование ресурсов
docker stats
```

---

## 🐛 Устранение неполадок

### Бот не запускается

```bash
# Проверьте логи
docker-compose logs

# Проверьте, что .env файл настроен правильно
cat .env

# Пересоберите контейнер
docker-compose down
docker-compose up -d --build
```

### Бот падает

```bash
# Увеличьте лимиты памяти в docker-compose.yml
# Добавьте в секцию services -> bot:
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M
```

### Недостаточно места на диске

```bash
# Очистка неиспользуемых Docker образов
docker system prune -a

# Проверка места
df -h
```

---

## 🔄 Обновление бота

Когда вы внесете изменения в код и запушите на GitHub:

```bash
# На сервере
cd ecoeats_bot
git pull
docker-compose up -d --build
```

---

## 📝 Полезные команды AWS CLI (опционально)

Установите AWS CLI для управления из терминала:

```bash
# Установка AWS CLI
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

# Настройка
aws configure
```

---

## 🎓 Дополнительные ресурсы

- [AWS Free Tier](https://aws.amazon.com/free/)
- [Docker Documentation](https://docs.docker.com/)
- [AWS EC2 Guide](https://docs.aws.amazon.com/ec2/)
- [AWS Lightsail Guide](https://lightsail.aws.amazon.com/ls/docs/)

---

## ✅ Чек-лист развертывания

- [ ] Создан аккаунт AWS
- [ ] Получен токен от @BotFather
- [ ] Создан EC2/Lightsail инстанс
- [ ] Установлен Docker на сервере
- [ ] Склонирован репозиторий
- [ ] Настроен .env файл с токеном
- [ ] Запущен бот через docker-compose
- [ ] Протестирован бот в Telegram
- [ ] Проверены логи на ошибки

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что токен правильный
3. Проверьте, что сервер доступен и работает
4. Проверьте, что Docker запущен: `docker ps`

Удачи с развертыванием! 🚀
