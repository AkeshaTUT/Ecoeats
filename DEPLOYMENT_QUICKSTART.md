# 🚀 Быстрое руководство по развертыванию на AWS

## Что было создано для AWS deployment:

### 📁 Новые файлы:
- ✅ `.gitignore` - исключает `.env`, `__pycache__` и другие файлы из Git
- ✅ `Dockerfile` - контейнеризация бота для AWS
- ✅ `docker-compose.yml` - оркестрация контейнера
- ✅ `AWS_DEPLOYMENT.md` - полная инструкция по развертыванию
- ✅ `deploy.sh` - скрипт автоматического развертывания (Linux/Mac)
- ✅ `deploy.ps1` - скрипт автоматического развертывания (Windows)

## 🎯 Следующие шаги:

### 1️⃣ Загрузка на GitHub

```powershell
# Инициализируйте Git репозиторий (если еще не сделали)
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: EcoEats Bot ready for AWS deployment"

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/YOUR_USERNAME/ecoeats_bot.git

# Загрузите на GitHub
git push -u origin main
```

### 2️⃣ Создание .env файла

Перед запуском создайте файл `.env` (используйте `.env.example` как шаблон):

```bash
BOT_TOKEN=ваш_токен_от_BotFather
ENVIRONMENT=production
LOG_LEVEL=INFO
```

**ВАЖНО:** Файл `.env` НЕ должен попадать в Git (уже в `.gitignore`)!

### 3️⃣ Развертывание на AWS

Выберите один из вариантов:

#### Вариант A: AWS EC2 (Рекомендуется)
1. Создайте EC2 instance (t2.micro - бесплатный tier)
2. Подключитесь по SSH
3. Клонируйте репозиторий
4. Запустите `./deploy.sh`

#### Вариант B: AWS Lightsail (Проще)
1. Создайте Lightsail instance ($3.50-5/месяц)
2. Подключитесь через веб-терминал
3. Клонируйте репозиторий
4. Запустите `./deploy.sh`

📖 **Полная инструкция в файле `AWS_DEPLOYMENT.md`**

## 🔧 Локальное тестирование перед AWS

```powershell
# Создайте .env файл
Copy-Item .env.example .env
# Отредактируйте .env и добавьте ваш BOT_TOKEN

# Запустите бота локально с Docker
docker-compose up -d

# Проверьте логи
docker-compose logs -f

# Остановка
docker-compose down
```

## 📊 Мониторинг на AWS

После развертывания:

```bash
# Просмотр логов
docker-compose logs -f

# Статус бота
docker-compose ps

# Перезапуск
docker-compose restart
```

## 🔄 Обновление бота на AWS

После изменений в коде:

```bash
# На сервере AWS
git pull
docker-compose up -d --build
```

## ✅ Что готово:
- ✅ Docker контейнеризация
- ✅ Автоматический перезапуск при падении
- ✅ Правильное логирование
- ✅ Безопасное хранение токенов
- ✅ Готово к работе 24/7
- ✅ Полная документация

## 🆘 Помощь

Если возникли вопросы, смотрите:
- `AWS_DEPLOYMENT.md` - полная инструкция по AWS
- `README.md` - основная документация проекта

Удачи! 🚀
