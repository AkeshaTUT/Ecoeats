# 🌱 EcoEats Bot - MVP

Экологичный Telegram бот для доставки еды с системой бонусов EcoPoints.

## 📋 Структура проекта

```
ecoeats_bot/
├── bot_with_db.py          # Основной файл бота (новая версия с БД)
├── bot_with_env.py         # Старая версия без БД
├── models.py               # ORM модели (SQLAlchemy)
├── database.py             # Сервис работы с БД
├── .env                    # Переменные окружения (BOT_TOKEN)
├── requirements.txt        # Зависимости
├── ecoeats.db             # SQLite база данных (создается автоматически)
└── README.md
```

## 🚀 Установка

### 1. Клонируйте или создайте проект
```bash
cd ecoeats_bot
```

### 2. Установите зависимости
```bash
pip install -r requirements.txt
```

### 3. Создайте файл `.env`
```bash
BOT_TOKEN=ваш_токен_от_botfather
```

### 4. Запустите бота
```bash
python bot_with_db.py
```

## 🗂️ Компоненты

### `models.py`
Определяет ORM модели:
- **User** - пользователи с их EcoPoints
- **Restaurant** - рестораны
- **Dish** - меню блюд
- **Order** - заказы пользователей
- **OrderItem** - товары в заказе
- **EcoPoint** - история бонусов

### `database.py`
**DatabaseService** - сервис для работы с БД:
- `get_or_create_user()` - создание/получение пользователя
- `get_restaurants()` - список ресторанов
- `get_dishes(restaurant_id)` - блюда ресторана
- `create_order()` - создание заказа и начисление бонусов
- `add_eco_points()` - добавление бонусов
- `get_user_stats()` - статистика пользователя

### `bot_with_db.py`
Основной бот с обработчиками:
- Команды: `/start`, `/menu`, `/cart`, `/bonus`
- Управление корзиной (сеансовая память)
- Работа с ресторанами и меню
- Система бонусов EcoPoints
- Возврат контейнеров

## 💡 Функциональность

### ✅ MVP функции
- [x] Регистрация пользователей в БД
- [x] Просмотр меню ресторанов
- [x] Добавление блюд в корзину
- [x] Выбор эко-упаковки
- [x] Оформление заказов
- [x] Система бонусов (EcoPoints)
- [x] Возврат контейнеров
- [x] Сохранение данных в SQLite

### 🔄 Корзина
Корзина хранится в памяти во время сеанса пользователя:
```python
user_carts = {
    user_id: [
        {"dish_id": 1, "price": 2500, "eco_packaging": True, ...},
        ...
    ]
}
```

При оформлении заказа корзина сохраняется в БД и очищается.

## 📊 База данных

SQLite база данных `ecoeats.db` содержит:

### Таблица `users`
- `id` - первичный ключ
- `telegram_id` - ID пользователя в Telegram
- `username` - имя пользователя
- `eco_points` - текущие бонусы
- `orders_count` - количество заказов
- `created_at` - дата регистрации

### Таблица `orders`
- `id` - ID заказа
- `user_id` - кто сделал заказ
- `total_amount` - сумма
- `eco_fee_total` - сумма эко-сборов
- `status` - статус (pending/completed)
- `created_at` - дата заказа

### Таблица `restaurants` и `dishes`
Содержат информацию о меню

### Таблица `eco_points`
Хранит историю начисления бонусов

## 💰 Система бонусов

- **10 EcoPoints** за каждое блюдо в эко-упаковке
- **5 EcoPoints** за возврат контейнера
- Бонусы сохраняются в БД и видны в профиле

## 🎛️ Переменные окружения

```env
# .env файл
BOT_TOKEN=8401864636:AAF3fDSK2Wd_wIdN313DrS5-s6ngnYTNRg0
```

## 📝 Команды бота

- `/start` - главное меню
- `/menu` - меню ресторанов
- `/cart` - просмотр корзины
- `/bonus` - просмотр бонусов

## 🎯 Обработчики кнопок

| Кнопка | Действие |
|--------|----------|
| 🍔 Меню ресторанов | Показ списка ресторанов |
| 🛒 Корзина | Просмотр заказов в корзине |
| 🌿 Мои бонусы | Просмотр EcoPoints |
| 🔄 Возврат контейнеров | Получение 5 бонусов |
| ℹ️ О сервисе | Информация о сервисе |

## 🔧 Технический стек

- **Фреймворк**: aiogram 3.4.1 (асинхронный Telegram API)
- **БД**: SQLAlchemy 2.0 + SQLite
- **Язык**: Python 3.14
- **Хранение конфига**: python-dotenv

## 📈 Будущие улучшения

- [ ] REST API для веб-интерфейса
- [ ] Веб-панель администратора
- [ ] Оплата через платежные системы
- [ ] Уведомления о статусе доставки
- [ ] Рейтинг ресторанов и отзывы
- [ ] Расширенная аналитика и отчеты
- [ ] Миграция на PostgreSQL для продакшена
- [ ] Docker контейнеризация

## 🛠️ Отладка

### Очистка БД
```bash
rm ecoeats.db
```
БД создастся заново при следующем запуске.

### Просмотр логов
```bash
# Логи выводятся в консоль при запуске
python bot_with_db.py
```

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

EcoEats Team
